// import ProfileCard from "./ProfileCard";
// import { membersData } from "../../public/members-data";
// import { membersData2 } from "../../public/members-data";
// import { membersData3 } from "../../public/members-data";
// import { membersData4 } from "../../public/members-data";
// import { membersData5 } from "../../public/members-data";

// const Members = () => {
//   return (
//     <div className="container mx-auto py-8">

//       <h2 className="text-3xl font-bold mb-4 mt-4">Founders</h2>
//       <div className="flex flex-wrap gap-8">
//         {membersData.map((member, index) => (
//           <ProfileCard key={index} {...member} />
//         ))}
//       </div>
//       <div className="flex flex-wrap gap-8">
//         {membersData2.map((member, index) => (
//           <ProfileCard key={index} {...member} />
//         ))}
//       </div>


//       <h2 className="text-3xl font-bold mb-4 mt-4">Core Committee</h2>
//       <div className="flex flex-wrap gap-8">
//         {membersData3.map((member, index) => (
//           <ProfileCard key={index} {...member} />
//         ))}
//       </div>


//       <h2 className="text-3xl font-bold mb-4 mt-4">Standing Committee</h2>
//       <div className="flex flex-wrap gap-8">
//         {membersData4.map((member, index) => (
//           <ProfileCard key={index} {...member} />
//         ))}
//       </div>


//       <h2 className="text-3xl font-bold mb-4 mt-4">Past Team</h2>
//       <div className="flex flex-wrap gap-8">
//         {membersData5.map((member, index) => (
//           <ProfileCard key={index} {...member} />
//         ))}
//       </div>


//     </div>
//   );
// };

// export default Members;


const Members = () => {
  return (
    <div>Members</div>
  )
}

export default Members